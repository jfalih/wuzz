import { HStack, VStack } from "@components/atoms";
import { memo } from "react";

const OutletList = memo(() => {
    return (
        <VStack fill>
            <HStack>

            </HStack>
        </VStack>
    )
});

export default OutletList;